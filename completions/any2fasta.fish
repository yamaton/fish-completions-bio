# Auto-generated with h2o

complete -c any2fasta -n "not __fish_seen_subcommand_from any2fasta" -s "h" -d "Print this help"
complete -c any2fasta -n "not __fish_seen_subcommand_from any2fasta" -s "v" -d "Print version and exit"
complete -c any2fasta -n "not __fish_seen_subcommand_from any2fasta" -s "q" -d "No output while running, only errors"
complete -c any2fasta -n "not __fish_seen_subcommand_from any2fasta" -s "n" -d "Replace ambiguous IUPAC letters with 'N'"
complete -c any2fasta -n "not __fish_seen_subcommand_from any2fasta" -s "l" -d "Lowercase the sequence"
complete -c any2fasta -n "not __fish_seen_subcommand_from any2fasta" -s "u" -d "Uppercase the sequence"



complete -k -c any2fasta -n __fish_use_subcommand -x -a any2fasta -d "[options] file.{gb,fa,fq,gff,gfa,clw,sth}[.gz,bz2,zip] > output.fasta"
