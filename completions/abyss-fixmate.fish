# Auto-generated with h2o

complete -c abyss-fixmate -l "no-qname" -d "set the qname to * [default]"
complete -c abyss-fixmate -l "qname" -d "do not alter the qname"
complete -c abyss-fixmate -l "all" -d "print all alignments"
complete -c abyss-fixmate -l "diff" -d "print alignments that align to different contigs [default]"
complete -c abyss-fixmate -s "l" -l "min-align" -d "the minimal alignment size [1]" -x
complete -c abyss-fixmate -s "s" -l "same" -d "write properly-paired reads to this file" -r
complete -c abyss-fixmate -s "h" -l "hist" -d "write the fragment size histogram to FILE" -r
complete -c abyss-fixmate -s "c" -l "cov" -d "write the physical coverage to FILE" -r
complete -c abyss-fixmate -s "v" -l "verbose" -d "display verbose output"
complete -c abyss-fixmate -l "help" -d "display this help and exit"
complete -c abyss-fixmate -l "version" -d "output version information and exit"
complete -c abyss-fixmate -l "db" -d "specify path of database repository in FILE" -r
complete -c abyss-fixmate -l "library" -d "specify library NAME for sqlite" -x
complete -c abyss-fixmate -l "strain" -d "specify strain NAME for sqlite" -x
complete -c abyss-fixmate -l "species" -d "specify species NAME for sqlite" -x
