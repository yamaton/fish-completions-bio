# Auto-generated with h2o

complete -c agat_sq_add_hash_tag.pl -l "gff" -l "file" -l "input" -d "STRING: Input GTF/GFF file."
complete -c agat_sq_add_hash_tag.pl -s "i" -l "interval" -d "Integer: 1 or 2."
complete -c agat_sq_add_hash_tag.pl -s "o" -l "output" -d "STRING: Output file."
complete -c agat_sq_add_hash_tag.pl -l "help" -s "h" -d "Display this helpful text."
