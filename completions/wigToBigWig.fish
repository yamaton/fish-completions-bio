# Auto-generated with h2o

complete -c wigToBigWig -o "blockSize" -d "Number of items to bundle in r-tree." -x
complete -c wigToBigWig -o "itemsPerSlot" -d "Number of data points bundled at lowest level." -x
complete -c wigToBigWig -o "clip" -d "If set just issue warning messages rather than dying if wig file contains items off end of chromosome or chromosomes that are not in the chrom.sizes file."
complete -c wigToBigWig -o "unc" -d "If set, do not use compression."
complete -c wigToBigWig -o "fixedSummaries" -d "If set, use a predefined sequence of summary levels."
complete -c wigToBigWig -o "keepAllChromosomes" -d "If set, store all chromosomes in b-tree."
