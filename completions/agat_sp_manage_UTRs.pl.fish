# Auto-generated with h2o

complete -c agat_sp_manage_UTRs.pl -l "gff" -l "ref" -l "reffile" -s "f" -d "Input GTF/GFF file."
complete -c agat_sp_manage_UTRs.pl -s "n" -s "t" -l "nb" -l "number" -d "Threshold of exon's number of the UTR."
complete -c agat_sp_manage_UTRs.pl -s "3" -l "three" -l "tree_prime_utr" -d "The threshold of the option <n> will be applied on the 3'UTR."
complete -c agat_sp_manage_UTRs.pl -s "5" -l "five" -l "five_prime_utr" -d "The threshold of the option <n> will be applied on the 5'UTR."
complete -c agat_sp_manage_UTRs.pl -s "b" -l "both" -l "bs" -d "The threshold of the option <n> will be applied on genes where the number of UTR exon (3' and 5' additioned) is over it."
complete -c agat_sp_manage_UTRs.pl -l "p" -l "plot" -d "Allows to create an histogram in pdf of UTR sizes distribution."
complete -c agat_sp_manage_UTRs.pl -l "out" -l "output" -s "o" -d "Output gff3 file where the gene incriminated will be write."
complete -c agat_sp_manage_UTRs.pl -l "help" -s "h" -d "Display this helpful text."
