# Auto-generated with h2o

complete -c agat_sp_extract_attributes.pl -l "gff" -s "f" -d "Input GTF/GFF file."
complete -c agat_sp_extract_attributes.pl -s "p" -s "t" -s "l" -d "primary tag option, case insensitive, list."
complete -c agat_sp_extract_attributes.pl -l "attribute" -l "att" -s "a" -d "attribute tag."
complete -c agat_sp_extract_attributes.pl -l "merge" -s "m" -d "By default the values of each attribute tag is writen in its dedicated file."
complete -c agat_sp_extract_attributes.pl -s "d" -d "By default when an attribute is not found for a feature, a dot (.) is reported."
complete -c agat_sp_extract_attributes.pl -s "o" -l "output" -l "out" -l "outfile" -d "Output GFF file."
complete -c agat_sp_extract_attributes.pl -s "h" -l "help" -d "Display this helpful text."
