# Auto-generated with h2o

complete -c agat_sq_manage_IDs.pl -l "gff" -l "file" -l "input" -d "STRING: Input GTF/GFF file."
complete -c agat_sq_manage_IDs.pl -l "of" -d "Output format, if no ouput format is given, the same as the input one detected will be used."
complete -c agat_sq_manage_IDs.pl -s "o" -l "output" -d "STRING: Output file."
complete -c agat_sq_manage_IDs.pl -l "help" -s "h" -d "Display this helpful text."
