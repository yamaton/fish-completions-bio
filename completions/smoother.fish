# Auto-generated with h2o

complete -c smoother -s "f" -l "file" -d "a file created by GPAT++" -r
complete -c smoother -s "o" -l "format" -d "format of input file, case sensitive." -r
complete -c smoother -s "w" -l "window" -d "size of genomic window in base pairs (default 5000)" -x
complete -c smoother -s "s" -l "step" -d "window step size in base pairs (default 1000)" -x
complete -c smoother -s "t" -l "truncate" -d "end last window at last position (zero based)" -x
