# Auto-generated with h2o

complete -c coverage_from_fastx -s "h" -l "help" -d "show this help message and exit"
complete -c coverage_from_fastx -l "coverage" -d "Calculate fraction of reads required for this coverage." -x
complete -c coverage_from_fastx -l "longest" -d "Use the longest reads when calculating fraction reads required for a given coverage."
