# Auto-generated with h2o

complete -c agat_sp_manage_IDs.pl -l "gff" -s "f" -d "Input GTF/GFF file."
complete -c agat_sp_manage_IDs.pl -l "gap" -d "Integer - Increment the next gene (level1 feature) suffix with this value."
complete -c agat_sp_manage_IDs.pl -l "ensembl" -d "Boolean - For an ID Ensembl like (e.g PREFIXG00000000022)."
complete -c agat_sp_manage_IDs.pl -l "prefix" -d "String - Add a specific prefix to the ID."
complete -c agat_sp_manage_IDs.pl -l "type_dependent" -d "Boolean - Activate type_dependent numbering."
complete -c agat_sp_manage_IDs.pl -l "collective" -d "Boolean - In the case of discontinuous features (i.e. a single feature that exists over multiple genomic locations like CDS, UTR) we set a uniq ID by default."
complete -c agat_sp_manage_IDs.pl -l "tair" -d "Boolean - Tair like Output:"
complete -c agat_sp_manage_IDs.pl -l "nb" -d "Integer - Start numbering to this value."
complete -c agat_sp_manage_IDs.pl -s "p" -s "t" -s "l" -d "primary tag option, case insensitive, list."
complete -c agat_sp_manage_IDs.pl -s "o" -l "output" -l "out" -l "outfile" -d "String - Output GFF file."
complete -c agat_sp_manage_IDs.pl -s "h" -l "help" -d "Boolean - Display this helpful text."
