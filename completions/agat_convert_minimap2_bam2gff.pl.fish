# Auto-generated with h2o

complete -c agat_convert_minimap2_bam2gff.pl -s "i" -l "input" -d "Input file in sam (.sam extension) or bam (.bam extension) format."
complete -c agat_convert_minimap2_bam2gff.pl -s "b" -l "bam" -d "To force to use the input file as sam file."
complete -c agat_convert_minimap2_bam2gff.pl -s "s" -l "sam" -d "To force to use the input file as sam file."
complete -c agat_convert_minimap2_bam2gff.pl -s "o" -l "out" -l "output" -d "Output GFF file."
complete -c agat_convert_minimap2_bam2gff.pl -s "h" -l "help" -d "Display this helpful text."
