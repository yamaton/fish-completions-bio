# Auto-generated with h2o

complete -c agat_sp_statistics.pl -l "gff" -s "i" -d "Input GTF/GFF file."
complete -c agat_sp_statistics.pl -l "gs" -s "f" -s "g" -d "This option inform about the genome size in oder to compute more statistics."
complete -c agat_sp_statistics.pl -s "d" -s "p" -d "When this option is used, an histogram of distribution of the features will be printed in pdf files."
complete -c agat_sp_statistics.pl -s "v" -l "verbose" -d "Verbose option."
complete -c agat_sp_statistics.pl -l "output" -s "o" -d "File where will be written the result."
complete -c agat_sp_statistics.pl -s "h" -l "help" -d "Display this helpful text."
