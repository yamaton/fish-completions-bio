# Auto-generated with h2o

complete -c agat_convert_embl2gff.pl -l "embl" -d "Input EMBL file that will be read"
complete -c agat_convert_embl2gff.pl -l "emblmygff3" -d "Bolean - Means that the EMBL flat file comes from the EMBLmyGFF3 software."
complete -c agat_convert_embl2gff.pl -l "primary_tag" -l "pt" -s "t" -d "List of \"primary tag\"."
complete -c agat_convert_embl2gff.pl -s "d" -d "Bolean - Means that primary tags provided by the option \"primary_tag\" will be discarded."
complete -c agat_convert_embl2gff.pl -s "k" -d "Bolean - Means that only primary tags provided by the option \"primary_tag\" will be kept."
complete -c agat_convert_embl2gff.pl -l "throw_fasta" -d "Bolean - Means that you do not want to keep the fasta sequence at the end of the gff output."
complete -c agat_convert_embl2gff.pl -s "o" -l "output" -l "out" -l "outfile" -l "gff" -d "Output GFF file."
complete -c agat_convert_embl2gff.pl -s "h" -l "help" -d "Display this helpful text."
