# Auto-generated with h2o

complete -c mgaps -s "C" -d "Check that fasta header labels alternately have \"Reverse\""
complete -c mgaps -s "d" -d "Fixed diagonal difference to join matches" -x
complete -c mgaps -s "e" -d "Use extent of match (end - start) rather than sum of piece lengths to determine length of cluster"
complete -c mgaps -s "f" -d "Fraction of separation for diagonal difference" -x
complete -c mgaps -s "l" -d "Minimum length of cluster match" -x
complete -c mgaps -s "s" -d "Maximum separation between matches in cluster" -x
