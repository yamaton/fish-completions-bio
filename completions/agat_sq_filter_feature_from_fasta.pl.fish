# Auto-generated with h2o

complete -c agat_sq_filter_feature_from_fasta.pl -l "gff" -l "file" -l "input" -d "STRING: Input GTF/GFF file."
complete -c agat_sq_filter_feature_from_fasta.pl -s "f" -l "fasta" -d "STRING: fasta file."
complete -c agat_sq_filter_feature_from_fasta.pl -s "v" -l "verbose" -d "For verbosity"
complete -c agat_sq_filter_feature_from_fasta.pl -s "o" -l "output" -d "STRING: Output file."
complete -c agat_sq_filter_feature_from_fasta.pl -l "help" -s "h" -d "Display this helpful text."
