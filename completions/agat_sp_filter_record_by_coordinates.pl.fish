# Auto-generated with h2o

complete -c agat_sp_filter_record_by_coordinates.pl -s "i" -l "input" -l "gtf" -l "gff" -d "Input GTF/GFF file"
complete -c agat_sp_filter_record_by_coordinates.pl -s "c" -l "coordinates" -l "tsv" -s "r" -l "ranges" -d "String - tsv file containing the coordinates."
complete -c agat_sp_filter_record_by_coordinates.pl -s "e" -l "exclude" -d "Select only the features fully containined within the coordinates, exclude the overlapping ones."
complete -c agat_sp_filter_record_by_coordinates.pl -s "o" -l "output" -d "Output folder."
complete -c agat_sp_filter_record_by_coordinates.pl -s "v" -l "verbose" -d "Verbosity."
complete -c agat_sp_filter_record_by_coordinates.pl -s "h" -l "help" -d "Display this helpful text."
