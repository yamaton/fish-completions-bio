# Auto-generated with h2o

complete -c primer3_core -l "default_version" -d "n=2 is the default, and directs primer3_core to use the latest default values (the ones documented here)." -r
complete -c primer3_core -l "format_output" -d "Generate user-oriented (rather than program-oriented) output."
complete -c primer3_core -l "strict_tags" -d "Generate a fatal error if there is any tag in the input that it does not recognize."
complete -c primer3_core -l "p3_settings_file" -d "Specifies a settings file, file_path." -r
complete -c primer3_core -l "echo_settings_file" -d "Print the input tags specified in the given settings file."
complete -c primer3_core -l "io_version" -d "(backward compatibility) --io_version=4 is the only legal value and the default ." -x
complete -c primer3_core -l "output" -d "Specifies the file where the output should be written." -r
complete -c primer3_core -l "error" -d "Specifies the file where the error messages should be written." -r
