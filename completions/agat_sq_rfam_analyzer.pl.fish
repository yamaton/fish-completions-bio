# Auto-generated with h2o

complete -c agat_sq_rfam_analyzer.pl -s "i" -l "gff" -l "file" -l "input" -d "STRING: Input GTF/GFF file(s)."
complete -c agat_sq_rfam_analyzer.pl -s "g" -l "genome" -d "That input is design to know the genome size in order to calculate the percentage of the genome represented by each kind of rfam-id."
complete -c agat_sq_rfam_analyzer.pl -s "o" -l "output" -d "STRING: Output file."
complete -c agat_sq_rfam_analyzer.pl -l "help" -s "h" -d "Display this helpful text."
