# Auto-generated with h2o

complete -c htsfile -s "c" -l "view" -d "Write textual form of FILEs to standard output"
complete -c htsfile -s "C" -l "copy" -d "Copy the exact contents of FILE to DESTFILE"
complete -c htsfile -s "h" -l "header-only" -d "Display only headers in view mode, not records"
complete -c htsfile -s "H" -l "no-header" -d "Suppress header display in view mode"
complete -c htsfile -s "v" -l "verbose" -d "Increase verbosity of warnings and diagnostics"
