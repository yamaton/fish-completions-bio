# Auto-generated with h2o

complete -c agat_sq_mask.pl -s "g" -l "gff" -o "ref" -d "Input GTF/GFF file."
complete -c agat_sq_mask.pl -s "f" -l "fasta" -d "Input fasta file that will be masked"
complete -c agat_sq_mask.pl -o "sm" -d "SoftMask option =>Sequences masked will be in lowercase"
complete -c agat_sq_mask.pl -o "hm" -d "HardMask option => Sequences masked will be replaced by a character."
complete -c agat_sq_mask.pl -s "o" -l "output" -d "Output GFF file."
complete -c agat_sq_mask.pl -s "h" -l "help" -d "Display this helpful text."
