# Auto-generated with h2o

complete -c subread-buildindex -s "o" -d "base name of the index to be created" -x
complete -c subread-buildindex -s "F" -d "build a full index for the reference genome."
complete -c subread-buildindex -s "B" -d "create one block of index."
complete -c subread-buildindex -s "M" -d "size of requested memory(RAM) in megabytes, 8000 by default." -x
complete -c subread-buildindex -s "f" -d "specify the threshold for removing uninformative subreads (highly repetitive 16mers in the reference)." -x
complete -c subread-buildindex -s "c" -d "build a color-space index."
complete -c subread-buildindex -s "v" -d "output version of the program."
