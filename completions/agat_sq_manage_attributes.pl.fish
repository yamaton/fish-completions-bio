# Auto-generated with h2o

complete -c agat_sq_manage_attributes.pl -l "gff" -s "f" -d "Input GTF/GFF file."
complete -c agat_sq_manage_attributes.pl -s "p" -l "type" -s "l" -d "primary tag option, case insensitive, list."
complete -c agat_sq_manage_attributes.pl -l "tag" -l "att" -d "Attributes with the tag specified will be removed from the feature type specified by the option p (primary tag)."
complete -c agat_sq_manage_attributes.pl -l "add" -d "Attribute with the tag specified will be added if doesn't exist."
complete -c agat_sq_manage_attributes.pl -l "cp" -d "When tags specied are with this form: tagName/newTagName."
complete -c agat_sq_manage_attributes.pl -l "overwrite" -d "When using --add parameter, if an attribute with the specificed tag already exists, it will not be modified."
complete -c agat_sq_manage_attributes.pl -l "value" -d "String."
complete -c agat_sq_manage_attributes.pl -l "strategy" -d "String."
complete -c agat_sq_manage_attributes.pl -s "o" -l "output" -l "out" -l "outfile" -d "Output GFF file."
complete -c agat_sq_manage_attributes.pl -s "h" -l "help" -d "Display this helpful text."
