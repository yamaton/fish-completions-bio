# Auto-generated with h2o

complete -c agat_convert_sp_gff2bed.pl -l "gff" -d "Input GFF3 file that will be read"
complete -c agat_convert_sp_gff2bed.pl -l "nc" -d "STRING - behaviour for non-coding features (e.g. recored wihtout CDS)."
complete -c agat_convert_sp_gff2bed.pl -l "sub" -d "Define the subfeature (level3, e.g exon,cds,utr,etc...) to report as blocks in the bed output."
complete -c agat_convert_sp_gff2bed.pl -s "h" -l "help" -d "Display this helpful text."
