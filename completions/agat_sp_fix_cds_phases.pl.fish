# Auto-generated with h2o

complete -c agat_sp_fix_cds_phases.pl -s "g" -l "gff" -o "ref" -d "Input GTF/GFF file."
complete -c agat_sp_fix_cds_phases.pl -o "fa" -l "fasta" -d "Input fasta file."
complete -c agat_sp_fix_cds_phases.pl -s "v" -l "verbose" -d "Add verbosity."
complete -c agat_sp_fix_cds_phases.pl -s "o" -l "output" -d "Output GFF file."
complete -c agat_sp_fix_cds_phases.pl -s "h" -l "help" -d "Display this helpful text."
