# Auto-generated with h2o

complete -c trim_alignments -s "h" -l "help" -d "show this help message and exit"
complete -c trim_alignments -s "r" -l "ref_name" -d "Reference to process, only needed if bams contain multiple references." -x
complete -c trim_alignments -s "o" -l "output_prefix" -d "Prefix for outputs." -x
complete -c trim_alignments -s "f" -l "reference_fasta" -d "Reference fasta to trim to alignment window." -x
