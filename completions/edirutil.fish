# Auto-generated with h2o

complete -c edirutil -o "db" -d "Work with the named Entrez database." -x
complete -c edirutil -o "web" -d "Pass the specified WebEnv cookie." -x
complete -c edirutil -o "key" -d "Work with the Nth result set associated with the specified WebEnv." -x
complete -c edirutil -o "count" -d "Expect N results." -x
complete -c edirutil -o "step" -d "Indicate the current pipeline depth." -x
complete -c edirutil -o "silent" -d "Suppress connection failure retry messages."
complete -c edirutil -o "verbose" -d "Display the ENTREZ_DIRECT XML field values at each step."
complete -c edirutil -o "debug" -d "Print the internal URL query and XML results of each step."
