# Auto-generated with h2o

complete -c barrnap -l "help" -d "This help"
complete -c barrnap -l "version" -d "Print version and exit"
complete -c barrnap -l "citation" -d "Print citation for referencing barrnap"
complete -c barrnap -l "kingdom" -d "Kingdom: mito arc bac euk (default 'bac')" -x
complete -c barrnap -l "quiet" -d "No screen output (default OFF)"
complete -c barrnap -l "threads" -d "Number of threads/cores/CPUs to use (default '1')" -x
complete -c barrnap -l "lencutoff" -d "Proportional length threshold to label as partial (default '0.8')" -x
complete -c barrnap -l "reject" -d "Proportional length threshold to reject prediction (default '0.25')" -x
complete -c barrnap -l "evalue" -d "Similarity e-value cut-off (default '1e-06')" -x
complete -c barrnap -l "incseq" -d "Include FASTA _input_ sequences in GFF3 output (default OFF)"
complete -c barrnap -l "outseq" -d "Save rRNA hit seqs to this FASTA file (default '')" -r
