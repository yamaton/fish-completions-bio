# Auto-generated with h2o

complete -c agat_sp_fix_fusion.pl -o "gff" -d "Input GTF/GFF file."
complete -c agat_sp_fix_fusion.pl -o "fa" -l "fasta" -d "Input fasta file."
complete -c agat_sp_fix_fusion.pl -l "ct" -l "codon" -l "table" -d "Codon table to use."
complete -c agat_sp_fix_fusion.pl -s "t" -l "threshold" -d "This is the minimum length of new protein predicted that will be taken in account."
complete -c agat_sp_fix_fusion.pl -s "s" -l "stranded" -d "By default we predict protein in UTR3 and UTR5 and in both direction."
complete -c agat_sp_fix_fusion.pl -s "v" -l "verbose" -d "Output verbose information."
complete -c agat_sp_fix_fusion.pl -s "o" -l "output" -l "out" -l "outfile" -d "Output GFF file."
complete -c agat_sp_fix_fusion.pl -s "h" -l "help" -d "Display this helpful text."
