complete -c vcfentropy -s f -l fasta-reference -d 'FASTA reference file to use to obtain flanking sequences'
complete -c vcfentropy -s w -l window-size -d 'Size of the window over which to calculate entropy'
