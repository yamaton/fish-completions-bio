# Auto-generated with h2o

complete -c j2x -o "input" -d "Read JSON from filename instead of standard input." -r
complete -c j2x -o "output" -d "Write XML to filename instead of standard output." -r
complete -c j2x -o "set" -d "Replace set wrapper tag." -x
complete -c j2x -o "rec" -d "Replace record wrapper tag." -x
complete -c j2x -o "nest" -d "Nested array naming policy." -x
complete -c j2x -o "timer" -d "Report processing duration and rate."
complete -c j2x -o "help" -d "Print usage information, complete with an example pipeline."
