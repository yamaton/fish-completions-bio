# Auto-generated with h2o

complete -c agat_sq_add_attributes_from_tsv.pl -l "gff" -d "STRING: Input GTF/GFF file."
complete -c agat_sq_add_attributes_from_tsv.pl -l "tsv" -d "STRING: Input tsv file"
complete -c agat_sq_add_attributes_from_tsv.pl -l "csv" -d "BOLEAN: Inform the script that the tsv input file is actually a csv (coma-separated)."
complete -c agat_sq_add_attributes_from_tsv.pl -s "v" -l "verbose" -d "BOLEAN: Add verbosity"
complete -c agat_sq_add_attributes_from_tsv.pl -s "o" -l "output" -d "STRING: Output file."
complete -c agat_sq_add_attributes_from_tsv.pl -l "help" -s "h" -d "Display this helpful text."
